客户对当前方案提出了修改意见。请判断并执行。

## 客户反馈

```
{{ user_feedback }}
```

## 当前方案（最新版）

```markdown
{{ current_design }}
```

## 任务

1. **判断意图**（3 种之一）：
   - **局部修订**：客户改某个具体章节（如"第三部分换 SaaS"、"预算压缩"）→ 用 `revise_section` 工具只改那个章节
   - **全文重写**：客户说"整篇重新写"/"推倒重来" → 重跑完整 design flow
   - **结构调整**：客户说"加一节"/"删除某节"/"把 X 调到末尾" → 优先 `revise_section` 处理重命名/重排；插入/删除走全文重写

2. **定位章节**（仅局部修订需要）：5 个 anchor 关键词：
   - `bg` = 背景
   - `goal` = 目标
   - `arch` = 架构
   - `deploy` = 部署
   - `budget` = 预算

   客户说"第三部分"通常是 `arch` 或 `deploy`，根据上下文判断。

3. **执行**：
   - 局部修订 → `revise_section(stage="design", name="design", section_id="<anchor>", new_content="<新内容>")`
   - 全文重写 → 用 `save_section` 写新版本
   - 结构调整 → 同上，尝试局部，失败则全文

4. **不要**：改客户没要求改的地方；引入新事实；丢失来源引用。

## 回复

用一句简短中文告诉客户改了什么、为什么。
