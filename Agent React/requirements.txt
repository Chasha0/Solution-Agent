# Solution Research Agent
# 客户面向的解决方案咨询 agent（Demo）
# 详细设计见 docs/superpowers/specs/2026-07-07-solution-research-agent-design.md

# === LLM（OpenAI 兼容）===
openai>=1.40.0
httpx>=0.27.0

# === UI ===
streamlit>=1.30.0
streamlit-extras>=0.4.0      # diff 渲染等

# === 数据模型 ===
pydantic>=2.5.0

# === KB / 向量检索 ===
chromadb>=0.5.0
# sentence-transformers 嵌入时启用（演示用同 provider 嵌入可不装）

# === 文档解析 ===
pymupdf>=1.24.0              # PDF
python-docx>=1.1.0            # Word
openpyxl>=3.1.0               # Excel

# === 模板 ===
jinja2>=3.1.0

# === PDF 渲染（P0 决策：fpdf2；WeasyPrint 在 Windows GTK 依赖失败）===
fpdf2>=2.7.0
# weasyprint>=62.0             # 备选（需 GTK 运行时，仅 Linux/macOS 友好）

# === 工具 ===
python-dotenv>=1.0.0
tiktoken>=0.7.0              # token 计数
